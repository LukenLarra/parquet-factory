name: BDD tests

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  bdd:
    name: BDD tests
    uses: LukenLarra/processing-tools/.github/workflows/bdd.yaml@bdd-framework-exec
    with:
      service: parquet-factory
      use_kafka: true
      use_minio: true
      use_pushgateway: true
      bdd_config: "bdd-configs/parquet-factory-framework.yml"